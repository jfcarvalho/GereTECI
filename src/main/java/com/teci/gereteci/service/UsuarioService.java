package com.teci.gereteci.service;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.teci.gereteci.model.*;
import com.teci.gereteci.repository.*;

@Service

public class UsuarioService {
/*	@Autowired
	private Usuarios repositorioUsuarios;
	@Autowired
	private Setores repositorioSetores;
	
	void addSetor(Setor setor, Integer id_usuario, Integer setor_id) 
	{
		Usuario usuario = repositorioUsuarios.getById(userId);
		Setor setor = repositorioSetores.getById(setor_id);
		
		
	}
*/
}
